// Strings.h

#pragma once

#include <iostream>
#include <map>
#include<algorithm>
#include <cctype>

namespace Data_Structure {

	void Duplicates(std::string in);
	void Anagram (std::string in1,std::string in2);
	void Reverse(std::string in);
}


